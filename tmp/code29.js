/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}/*
Author: Rajesh Vedhamurthy
Description: 
Round a given number to the given number of places.
For ex: (10.3455,2) will return 10.34
*/

function roundTo(Num, Places) {
   if (Places > 0) {
      if ((Num.toString().length - Num.toString().lastIndexOf('.')) > (Places + 1)) {
         var Rounder = Math.pow(10, Places);
         return Math.round(Num * Rounder) / Rounder;
      }
      else return Num;
   }
   else return Math.round(Num);
}